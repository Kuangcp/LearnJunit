apply plugin: 'java'

buildDir = 'out/build'

sourceCompatibility = 1.8
targetCompatibility = 1.8

repositories {
  mavenLocal()
  jcenter()
}

dependencies {
  implementation('junit.kcp:junit:3.8.1-SNAPSHOT')
}